{
  "response": {
    "totalRecord": "{{totalRecord}}",
    "data": [
     {{#each data}}
      {
        "isActive": "{{isActive}}",
        "code": "{{code}}",
        "name": "{{name}}",
        "description": "{{description}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}