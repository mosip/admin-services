{
  "response": {
    "data": [
     {{#each data}}
      {
        "isActive": "{{isActive}}",
        "id": "{{id}}",
        "name": "{{name}}",
        "model": "{{model}}",
        "brand": "{{brand}}",
        "machineTypeCode": "{{machineTypeCode}}",
        "minDriverversion": "{{minDriverversion}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}